{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Only what the app actually needs: a folder picker, and opening a URL in the browser. File reading, trashing and revealing all go through our own commands, which check that the open tree is a live scan first.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "opener:allow-open-url"
  ]
}
